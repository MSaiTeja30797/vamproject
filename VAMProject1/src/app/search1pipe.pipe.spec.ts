import { Search1pipePipe } from './search1pipe.pipe';

describe('Search1pipePipe', () => {
  it('create an instance', () => {
    const pipe = new Search1pipePipe();
    expect(pipe).toBeTruthy();
  });
});
